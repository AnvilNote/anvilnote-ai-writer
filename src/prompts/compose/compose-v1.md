# Compose task v1

Create editable AnvilNote document content that follows the user's instruction. Use current-document context only when supplied. Do not overwrite an existing document or title automatically. If essential information is absent, remain conservative and record a warning instead of inventing details.
