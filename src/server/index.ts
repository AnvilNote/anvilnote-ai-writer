/**
 * Phase 1 reserves this trusted-runtime entrypoint. Provider SDK adapters,
 * prompt loading, and execution orchestration are added in later phases.
 */
export const AI_WRITER_SERVER_API_VERSION = 1;
